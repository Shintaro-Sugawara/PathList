VSVersionInfo(
    ffi=FixedFileInfo(
        filevers=(1,0,0,0),       # 実行ファイルのバージョン番号 (major, minor, build, revision)
        prodvers=(1,0,0,0),       # 製品バージョン番号 (major, minor, build, revision)
        mask=0x3f,
        flags=0x0,
        OS=0x4,
        fileType=0x1,             # 実行ファイル
        subtype=0x0,
        date=(0,0)
    ),
    kids=[
        StringFileInfo([
            # 日本語リソースのみ
            StringTable(
                '040904b0',  # 英語（米国）・Unicode
                [
                    # PublisherName: 発行者名。キーPublisherNameは存在せず、CompanyNameがPublisherNameに相当
                    # そのため個人でも個人名をCompanyNameに入れる（署名やストア表示に利用）
                    StringStruct('CompanyName', 'S.Sugawara'),

                    # FileDescription: ファイルの説明（右クリックのプロパティ「全般」タブと「詳細」タブで表示）
                    StringStruct('FileDescription', 'Drop folders onto app icon to output list'),

                    # FileVersion: 実行ファイルのバージョン
                    StringStruct('FileVersion', '1.0.0.0'),

                    # InternalName: 内部識別用の名前
                    StringStruct('InternalName', 'PathListPro'),

                    # OriginalFilename: ファイル本体の名前
                    StringStruct('OriginalFilename', 'PathListPro.exe'),

                    # ProductName: 製品名
                    StringStruct('ProductName', 'Path List Pro'),

                    # ProductVersion: 製品バージョン
                    StringStruct('ProductVersion', '1.0.0.0'),

                    # DisplayName: スタートメニューやアプリ一覧での表示名
                    StringStruct('DisplayName', 'Path List Pro'),

                    # LegalCopyright: 著作権表示（右クリックのプロパティ「詳細」タブで表示）
                    StringStruct('LegalCopyright', 'Copyright (C) 2025 S.Sugawara')
                ]
            )
        ]),

        VarFileInfo([
            VarStruct('Translation', [1033, 1200])  # 英語（米国）
        ])
    ]
)
